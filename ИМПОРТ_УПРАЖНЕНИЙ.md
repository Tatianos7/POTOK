# 📥 Инструкция по импорту упражнений

## ✅ Шаг 1: SQL скрипт выполнен успешно!

Если вы видите "Success. No rows returned" — это означает, что все таблицы созданы успешно.

## 📋 Шаг 2: Проверка таблиц

Выполните в Supabase SQL Editor:

```sql
-- Проверка таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
  AND table_name IN (
    'exercise_categories',
    'muscles',
    'exercises',
    'exercise_muscles',
    'workout_days',
    'workout_entries'
  )
ORDER BY table_name;
```

Должно вернуться **6 таблиц**.

## 🚀 Шаг 3: Импорт упражнений

### Вариант 1: Через консоль браузера (РЕКОМЕНДУЕТСЯ)

1. **Откройте приложение в браузере**
   - Запустите `npm run dev` (если еще не запущено)
   - Откройте приложение в браузере (обычно http://localhost:5173)

2. **Откройте DevTools**
   - Нажмите `F12` или `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
   - Перейдите на вкладку **Console**

3. **Выполните импорт**
   Скопируйте и вставьте в консоль:

```javascript
// Проверяем, доступна ли функция
if (window.seedExercises) {
  window.seedExercises().then(() => {
    console.log('✅ Упражнения успешно импортированы!');
  }).catch((error) => {
    console.error('❌ Ошибка импорта:', error);
  });
} else {
  console.error('❌ Функция seedExercises не найдена. Убедитесь, что файл seedExercises.ts загружен.');
}
```

### Вариант 2: Через временную страницу

Если вариант 1 не работает, создайте временную страницу для импорта:

1. **Создайте файл** `src/pages/ImportExercises.tsx`:

```typescript
import { useEffect } from 'react';
import { seedExercises } from '../utils/seedExercises';

const ImportExercises = () => {
  useEffect(() => {
    const importData = async () => {
      try {
        console.log('Начинаю импорт упражнений...');
        await seedExercises();
        console.log('✅ Упражнения успешно импортированы!');
        alert('✅ Упражнения успешно импортированы!');
      } catch (error) {
        console.error('❌ Ошибка импорта:', error);
        alert('❌ Ошибка импорта: ' + (error as Error).message);
      }
    };
    importData();
  }, []);

  return (
    <div className="p-4">
      <h1>Импорт упражнений</h1>
      <p>Проверьте консоль браузера для результатов.</p>
    </div>
  );
};

export default ImportExercises;
```

2. **Добавьте маршрут** в `src/App.tsx`:

```typescript
import ImportExercises from './pages/ImportExercises';

// В секции Routes добавьте:
<Route path="/import-exercises" element={<ImportExercises />} />
```

3. **Откройте в браузере**: `http://localhost:5173/import-exercises`

4. **Проверьте консоль** — должны увидеть сообщение об успешном импорте

### Вариант 3: Через SQL напрямую (если TypeScript не работает)

Если оба варианта выше не работают, можно импортировать данные напрямую через SQL. Но это займет больше времени, так как нужно будет вставить все упражнения вручную.

## ✅ Шаг 4: Проверка импорта

После успешного импорта проверьте данные:

```sql
-- Проверка категорий
SELECT COUNT(*) as categories_count FROM exercise_categories;
-- Должно быть: 7 (Плечи, Руки, Грудь, Спина, Ноги, Пресс, Кардио)

-- Проверка мышц
SELECT COUNT(*) as muscles_count FROM muscles;
-- Должно быть: ~50+ различных мышц

-- Проверка упражнений
SELECT COUNT(*) as exercises_count FROM exercises;
-- Должно быть: ~200+ упражнений

-- Проверка связей упражнений и мышц
SELECT COUNT(*) as links_count FROM exercise_muscles;
-- Должно быть: ~200+ связей
```

## 🎯 Шаг 5: Проверка в приложении

1. **Откройте дневник тренировок**
   - Перейдите в приложении на страницу "ДНЕВНИК ТРЕНИРОВОК"

2. **Нажмите кнопку "+" (ДОБАВИТЬ)**

3. **Должны увидеть список категорий:**
   - Плечи
   - Руки
   - Грудь
   - Спина
   - Ноги
   - Пресс
   - Кардио

4. **Выберите категорию** — должен открыться список упражнений

5. **Попробуйте поиск** — введите название упражнения

## ⚠️ Возможные проблемы

### Проблема 1: "window.seedExercises is not a function"

**Решение:**
- Убедитесь, что файл `src/utils/seedExercises.ts` существует
- Проверьте, что в конце файла есть строка: `(window as any).seedExercises = seedExercises;`
- Перезагрузите страницу (Ctrl+R или Cmd+R)

### Проблема 2: "Supabase не инициализирован"

**Решение:**
- Проверьте файл `.env.local` или `.env`
- Убедитесь, что есть переменные:
  ```
  VITE_SUPABASE_URL=ваш_url
  VITE_SUPABASE_ANON_KEY=ваш_key
  ```
- Перезапустите dev сервер после изменения .env

### Проблема 3: Ошибки при импорте

**Решение:**
- Откройте консоль браузера (F12)
- Посмотрите детали ошибки
- Проверьте, что таблицы созданы (см. Шаг 2)
- Убедитесь, что у пользователя есть права на INSERT

## 📞 Если ничего не помогает

1. **Проверьте логи Supabase:**
   - Перейдите в Supabase Dashboard
   - Откройте раздел **Logs** → **Postgres Logs**
   - Посмотрите, есть ли ошибки

2. **Проверьте RLS политики:**
   - Убедитесь, что политики созданы корректно
   - Проверьте через: **Authentication** → **Policies**

3. **Попробуйте импортировать вручную:**
   - Можно создать несколько упражнений вручную через SQL для проверки

## ✅ Готово!

После успешного импорта система готова к использованию:
- ✅ Таблицы созданы
- ✅ Упражнения импортированы
- ✅ Можно добавлять упражнения в дневник тренировок

