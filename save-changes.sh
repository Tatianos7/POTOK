#!/bin/bash

# Скрипт для сохранения изменений в Git и деплоя на GitHub Pages

set -e

echo "🚀 Начинаю сохранение изменений в Git и деплой на GitHub Pages..."

cd "$(dirname "$0")"

# Проверяем, что мы в правильной директории
if [ ! -f "package.json" ]; then
  echo "❌ Ошибка: package.json не найден."
  exit 1
fi

# Инициализируем git, если нужно
if [ ! -d ".git" ]; then
  echo "📦 Инициализирую git репозиторий..."
  git init
fi

# Добавляем все изменения
echo "📝 Добавляю все изменения..."
git add -A

# Показываем статус
echo ""
echo "📊 Статус изменений:"
git status --short

# Создаем коммит
echo ""
echo "💾 Создаю коммит..."
git commit -m "feat: улучшена работа дневника питания

- Добавлен встроенный календарь для выбора даты
- Реализованы оптимистичные обновления UI (мгновенное отображение)
- Исправлены ошибки Supabase (поле planned)
- Улучшена загрузка данных (приоритет localStorage)
- Исправлено удаление продуктов (работает с первого раза)
- Добавлена синхронизация данных в фоне" || echo "⚠️  Коммит не создан (возможно, нет изменений)"

# Проверяем наличие удаленного репозитория
echo ""
echo "🔍 Проверяю удаленный репозиторий..."
if git remote | grep -q origin; then
  REMOTE_URL=$(git remote get-url origin)
  echo "✅ Найден удаленный репозиторий: $REMOTE_URL"
  
  # Определяем текущую ветку
  CURRENT_BRANCH=$(git branch --show-current 2>/dev/null || echo "main")
  if [ -z "$CURRENT_BRANCH" ]; then
    CURRENT_BRANCH="main"
    git branch -M main 2>/dev/null || true
  fi
  
  echo "📤 Отправляю изменения в ветку $CURRENT_BRANCH..."
  git push -u origin "$CURRENT_BRANCH" 2>&1 || {
    echo "⚠️  Не удалось отправить изменения автоматически."
    echo ""
    echo "Выполни вручную:"
    echo "  git push -u origin $CURRENT_BRANCH"
  }
else
  echo "⚠️  Удаленный репозиторий не настроен."
  echo ""
  echo "Для настройки выполни:"
  echo "  git remote add origin https://github.com/ТВОЙ_USERNAME/POTOK.git"
  echo "  git branch -M main"
  echo "  git push -u origin main"
fi

echo ""
echo "✅ Изменения сохранены в Git!"
echo ""
echo "🌐 GitHub Pages:"
echo "Если настроен GitHub Actions, деплой произойдет автоматически."
echo "Проверь вкладку 'Actions' в репозитории на GitHub."

