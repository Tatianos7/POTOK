# 🚀 Быстрый старт - Импорт упражнений

## ✅ Что уже сделано

1. ✅ SQL скрипт выполнен успешно (таблицы созданы)
2. ✅ Страница импорта создана: `/import-exercises`
3. ✅ Маршрут добавлен в приложение
4. ✅ Функция `seedExercises` доступна через `window`

## 🎯 Как импортировать упражнения

### Способ 1: Через страницу импорта (РЕКОМЕНДУЕТСЯ)

1. **Запустите приложение:**
   ```bash
   npm run dev
   ```

2. **Откройте в браузере:**
   ```
   http://localhost:5173/import-exercises
   ```

3. **Импорт запустится автоматически:**
   - Вы увидите индикатор загрузки
   - Дождитесь завершения (может занять 1-2 минуты)
   - После успешного импорта появится кнопка "Перейти в дневник тренировок"

### Способ 2: Через консоль браузера

1. **Откройте приложение в браузере:**
   ```
   http://localhost:5173
   ```

2. **Откройте DevTools:**
   - Нажмите `F12` или `Cmd+Option+I` (Mac) / `Ctrl+Shift+I` (Windows)
   - Перейдите на вкладку **Console**

3. **Выполните команду:**
   ```javascript
   window.seedExercises()
   ```

4. **Проверьте результат:**
   - В консоли должно появиться: "Импорт упражнений завершен успешно!"
   - Если есть ошибки, они будут показаны в консоли

## ✅ Проверка после импорта

### В Supabase SQL Editor:

```sql
-- Проверка категорий (должно быть 7)
SELECT COUNT(*) as categories_count FROM exercise_categories;

-- Проверка мышц (должно быть ~50+)
SELECT COUNT(*) as muscles_count FROM muscles;

-- Проверка упражнений (должно быть ~200+)
SELECT COUNT(*) as exercises_count FROM exercises;

-- Проверка связей (должно быть ~200+)
SELECT COUNT(*) as links_count FROM exercise_muscles;
```

### В приложении:

1. **Откройте дневник тренировок:**
   - Перейдите на страницу "ДНЕВНИК ТРЕНИРОВОК"

2. **Нажмите кнопку "+" (ДОБАВИТЬ)**

3. **Должны увидеть категории:**
   - Плечи
   - Руки
   - Грудь
   - Спина
   - Ноги
   - Пресс
   - Кардио

4. **Выберите категорию** — должен открыться список упражнений

5. **Попробуйте поиск** — введите название упражнения

## ⚠️ Возможные проблемы

### Проблема: "Supabase не инициализирован"

**Решение:**
1. Проверьте файл `.env.local` или `.env`
2. Убедитесь, что есть переменные:
   ```
   VITE_SUPABASE_URL=ваш_url
   VITE_SUPABASE_ANON_KEY=ваш_key
   ```
3. Перезапустите dev сервер:
   ```bash
   # Остановите сервер (Ctrl+C)
   npm run dev
   ```

### Проблема: "window.seedExercises is not a function"

**Решение:**
1. Убедитесь, что страница `/import-exercises` была открыта хотя бы раз
2. Или используйте страницу импорта вместо консоли
3. Перезагрузите страницу (Ctrl+R или Cmd+R)

### Проблема: Ошибки при импорте

**Решение:**
1. Проверьте консоль браузера (F12) для деталей ошибки
2. Убедитесь, что таблицы созданы (см. проверку выше)
3. Проверьте RLS политики в Supabase Dashboard

## 🎉 Готово!

После успешного импорта:
- ✅ Все упражнения добавлены в базу данных
- ✅ Можно добавлять упражнения в дневник тренировок
- ✅ Поиск работает
- ✅ Можно создавать пользовательские упражнения

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи Supabase: **Dashboard** → **Logs** → **Postgres Logs**
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все таблицы созданы корректно

