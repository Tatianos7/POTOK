# 📝 Инструкция по сохранению изменений в GitHub

## Способ 1: Автоматический скрипт (рекомендуется)

Открой терминал в папке проекта и выполни:

```bash
bash save-to-github.sh
```

Скрипт автоматически:
- Добавит все изменения
- Создаст коммит
- Отправит в GitHub

## Способ 2: Ручное выполнение команд

Выполни команды по порядку:

### Шаг 1: Перейди в папку проекта
```bash
cd /Users/urijurij/Desktop/POTOK
```

### Шаг 2: Инициализируй git (если еще не сделано)
```bash
git init
```

### Шаг 3: Добавь все файлы
```bash
git add -A
```

### Шаг 4: Проверь, что файлы добавлены
```bash
git status
```

Должны увидеть список измененных/новых файлов.

### Шаг 5: Создай коммит
```bash
git commit -m "Интеграция профиля пользователя и замеров с Supabase"
```

### Шаг 6: Проверь удаленный репозиторий
```bash
git remote -v
```

Если ничего не выводится, значит репозиторий не настроен. Перейди к Шагу 7.

Если видишь URL (например, `https://github.com/username/POTOK.git`), перейди к Шагу 8.

### Шаг 7: Настрой удаленный репозиторий (если еще не настроен)

Замени `ТВОЙ_USERNAME` на свой GitHub username:

```bash
git remote add origin https://github.com/ТВОЙ_USERNAME/POTOK.git
git branch -M main
```

### Шаг 8: Отправь изменения в GitHub

```bash
git push -u origin main
```

Если это первый раз, может потребоваться авторизация. GitHub попросит ввести логин и пароль (или токен).

### Шаг 9: Проверь на GitHub

1. Открой свой репозиторий на GitHub
2. Убедись, что все файлы появились
3. Проверь, что коммит создан

## Настройка GitHub Pages

После того как код отправлен в GitHub:

1. Открой репозиторий на GitHub
2. Перейди в **Settings** (Настройки)
3. В левом меню найди **Pages**
4. В разделе **Source** выбери **GitHub Actions**
5. Сохрани изменения

GitHub Actions автоматически соберет и задеплоит проект.

## Проверка деплоя

После настройки GitHub Actions:
- Перейди во вкладку **Actions** в репозитории
- Должен появиться workflow "Deploy to GitHub Pages"
- Дождись завершения (зеленая галочка)
- Приложение будет доступно по адресу: `https://ТВОЙ_USERNAME.github.io/POTOK/`

## Если возникли проблемы

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ТВОЙ_USERNAME/POTOK.git
```

### Ошибка: "failed to push"
Попробуй:
```bash
git push -u origin main --force
```
⚠️ Внимание: `--force` перезапишет удаленную ветку. Используй только если уверен.

### Ошибка авторизации
Создай Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Создай новый токен с правами `repo`
3. Используй токен вместо пароля при push

