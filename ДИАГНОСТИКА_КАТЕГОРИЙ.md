# 🔍 Диагностика проблемы с категориями

## Проблема: Категории не появляются

Если категории не отображаются в дневнике тренировок, выполните следующие шаги для диагностики:

## 📋 Шаг 1: Проверка данных в Supabase

### В Supabase SQL Editor выполните:

```sql
-- Проверка категорий
SELECT * FROM exercise_categories ORDER BY "order";

-- Должно вернуться 7 категорий:
-- Плечи, Руки, Грудь, Спина, Ноги, Пресс, Кардио
```

**Если категорий нет:**
- Нужно импортировать упражнения (см. Шаг 2)

**Если категории есть:**
- Проблема в загрузке данных (см. Шаг 3)

## 📋 Шаг 2: Импорт упражнений

### Вариант A: Через страницу импорта

1. Откройте в браузере: `http://localhost:5173/import-exercises`
2. Дождитесь завершения импорта
3. Проверьте консоль браузера (F12) на наличие ошибок

### Вариант B: Через консоль браузера

1. Откройте приложение в браузере
2. Откройте DevTools (F12) → Console
3. Выполните:
```javascript
window.seedExercises()
```

4. Дождитесь сообщения: "Импорт упражнений завершен успешно!"

## 📋 Шаг 3: Проверка загрузки категорий

### В консоли браузера (F12):

1. **Проверьте логи загрузки:**
   - Откройте вкладку Console
   - Перезагрузите страницу дневника тренировок
   - Должны увидеть: `[Workouts] Загрузка категорий...`
   - Затем: `[Workouts] Загружено категорий: X`

2. **Проверьте данные в базе:**
   ```javascript
   window.checkExercisesData()
   ```
   
   Должно показать:
   ```
   📊 Проверка данных упражнений:
     Категории: 7
     Мышцы: ~50+
     Упражнения: ~200+
     Связи: ~200+
   ```

3. **Проверьте ошибки:**
   - Если есть ошибки в консоли, скопируйте их
   - Частые ошибки:
     - `Supabase не инициализирован` → проверьте .env файл
     - `RLS policy violation` → проверьте RLS политики
     - `relation does not exist` → таблицы не созданы

## 📋 Шаг 4: Проверка RLS политик

### В Supabase Dashboard:

1. Перейдите в **Authentication** → **Policies**
2. Найдите таблицу `exercise_categories`
3. Убедитесь, что есть политика: **"Anyone can read exercise categories"**
4. Если политики нет, выполните SQL:

```sql
CREATE POLICY "Anyone can read exercise categories"
  ON exercise_categories FOR SELECT
  USING (true);
```

## 📋 Шаг 5: Проверка переменных окружения

### Проверьте файл `.env.local` или `.env`:

```env
VITE_SUPABASE_URL=ваш_url
VITE_SUPABASE_ANON_KEY=ваш_key
```

**Если переменных нет:**
1. Получите их из Supabase Dashboard:
   - **Settings** → **API**
   - Скопируйте `Project URL` и `anon public` key
2. Создайте файл `.env.local` в корне проекта
3. Добавьте переменные
4. Перезапустите dev сервер:
   ```bash
   # Остановите (Ctrl+C)
   npm run dev
   ```

## 📋 Шаг 6: Ручная проверка загрузки

### В консоли браузера:

```javascript
// Проверка Supabase клиента
console.log('Supabase:', window.supabase || 'не найден');

// Проверка загрузки категорий
import { exerciseService } from './src/services/exerciseService';
exerciseService.getCategories().then(cats => {
  console.log('Категории:', cats);
}).catch(err => {
  console.error('Ошибка:', err);
});
```

## 🔧 Быстрое решение

Если ничего не помогает, выполните в Supabase SQL Editor:

```sql
-- Удаляем и пересоздаем категории вручную
DELETE FROM exercise_categories;

INSERT INTO exercise_categories (name, "order") VALUES
  ('Плечи', 1),
  ('Руки', 2),
  ('Грудь', 3),
  ('Спина', 4),
  ('Ноги', 5),
  ('Пресс', 6),
  ('Кардио', 7);
```

Затем перезагрузите страницу дневника тренировок.

## ✅ После исправления

1. Откройте дневник тренировок
2. Нажмите кнопку "+" (ДОБАВИТЬ)
3. Должны увидеть 7 категорий
4. Выберите категорию → должен открыться список упражнений

## 📞 Если проблема не решена

1. Скопируйте все ошибки из консоли браузера
2. Проверьте логи Supabase: **Dashboard** → **Logs** → **Postgres Logs**
3. Убедитесь, что:
   - Таблицы созданы (6 таблиц)
   - RLS политики настроены
   - Переменные окружения установлены
   - Импорт упражнений выполнен

